services:
  - type: web
    name: asr-server
    runtime: node
    plan: standard
    buildCommand: >
      cd server &&
      npm install --include=dev &&
      npm run build
    startCommand: cd server && node dist/src/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    region: oregon
